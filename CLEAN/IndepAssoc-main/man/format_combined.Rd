% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_combined}
\alias{format_combined}
\title{Format a combined multi-outcome comparison table for publication}
\usage{
format_combined(combined_comparison, digits = 2, p_digits = 3)
}
\arguments{
\item{combined_comparison}{A data.frame with an \code{Outcome} column plus the raw
comparison columns (see \code{format_comparison()}): multiple outcomes'
comparison rows stacked together, each tagged with its outcome label.}

\item{digits}{Number of decimal places for \code{estimate}, \code{conf_low}, and
\code{conf_high} (default 2).}

\item{p_digits}{Number of decimal places for \code{p_value} (default 3); values
below \code{10^-p_digits} are rendered as \code{"<0.001"} (etc.).}
}
\value{
A data.frame with columns \code{Outcome}, \code{Method}, \code{Estimate}, \verb{95\% CI},
\code{p-value}, and \code{n}. Because the rows may mix binary and continuous
outcomes, the estimate column keeps the generic name \code{Estimate} — read it
together with the \code{Outcome} column (an OR for binary outcomes, a mean
difference in the outcome's units for continuous ones).
}
\description{
Format a combined multi-outcome comparison table for publication
}
\examples{
data(example_cohort)
res <- run_pipeline(
  data = example_cohort,
  exposure = "exposure",
  covariates = c("age", "diabetes", "hypertension", "bmi"),
  outcome = "outcome_binary",
  type = "binary",
  methods = c("regression", "matching", "stratification", "iptw", "aipw")
)
combined <- rbind(
  cbind(Outcome = "30-day mortality", res$comparison),
  cbind(Outcome = "Hospital length of stay (days)", res$comparison)
)
format_combined(combined)
}
