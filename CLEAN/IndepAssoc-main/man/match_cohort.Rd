% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{match_cohort}
\alias{match_cohort}
\title{Match Cohort Using Propensity Scores}
\usage{
match_cohort(
  ps_model,
  method = "nearest",
  caliper = 0.2,
  ratio = 1,
  replace = FALSE,
  distance = "logit",
  seed = NULL
)
}
\arguments{
\item{ps_model}{An \code{IndepPSModel} object returned by \code{build_ps_model()}.}

\item{method}{Matching method passed to \code{MatchIt::matchit()}. Default \code{"nearest"}.}

\item{caliper}{Caliper width in SD of logit(PS). Default \code{0.2}.}

\item{ratio}{Number of control matches per treated unit. Default \code{1}.}

\item{replace}{Whether to match with replacement. Only \code{FALSE} is supported;
\code{TRUE} errors immediately (see Details). Default \code{FALSE}.}

\item{distance}{PS distance metric. Default \code{"logit"}.}

\item{seed}{Integer passed to \code{set.seed()} before matching; required for
reproducible matching when tie-breaking or MatchIt internals consume
randomness. Default \code{NULL} (no seeding).}
}
\value{
A list of class \code{"IndepMatch"} with elements:
\describe{
\item{match_obj}{The \code{MatchIt} match object.}
\item{data}{Matched data frame.}
\item{ps_model}{The input \code{IndepPSModel} object.}
}
}
\description{
Performs propensity score matching via \code{MatchIt}.
}
\details{
\code{replace = TRUE} is not supported: matching with replacement produces no
match-pair identifier from \code{MatchIt::match.data()}, which the paired
downstream functions (balance tables, paired tests, and the conditional-logit
matching estimator) all require. Pass \code{replace = FALSE} (the default), or use
\code{find_matching_data_summary()} when replacement matching is needed.
}
\examples{
data(example_cohort)
ps <- build_ps_model(example_cohort, "exposure",
                     c("age", "diabetes", "hypertension", "bmi"))
matched <- match_cohort(ps, caliper = 0.2, ratio = 1)
head(matched$data)

}
