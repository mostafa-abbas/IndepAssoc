% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_comparison.R
\name{plot_comparison}
\alias{plot_comparison}
\title{Forest Plot of Method Comparison Results}
\usage{
plot_comparison(comparison, log_scale = TRUE)
}
\arguments{
\item{comparison}{A data.frame from \code{run_pipeline()}'s \verb{$comparison}
element (columns \code{method}, \code{estimate}, \code{conf_low}, \code{conf_high}, ...).}

\item{log_scale}{Logical; \code{TRUE} (default) plots on the log scale with a
reference line at 1 (binary outcomes, estimates are ORs). \code{FALSE} plots
on the linear scale with a reference line at 0.}
}
\value{
A \code{ggplot} object, returned visibly (it does not print itself).
Calling it bare at the console or in an R Markdown chunk renders the plot
once.
}
\description{
Forest Plot of Method Comparison Results
}
\examples{
data(example_cohort)
res <- run_pipeline(
  data = example_cohort,
  exposure = "exposure",
  covariates = c("age", "diabetes", "hypertension", "bmi"),
  outcome = "outcome_binary",
  type = "binary",
  methods = c("regression", "iptw")
)
plot_comparison(res$comparison, log_scale = TRUE)
}
