% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_comparison}
\alias{format_comparison}
\title{Format a comparison table for publication}
\usage{
format_comparison(comparison, digits = 2, p_digits = 3)
}
\arguments{
\item{comparison}{A data.frame from \code{run_pipeline()}'s \verb{$comparison} element
(columns \code{method}, \code{type}, \code{estimate}, \code{conf_low}, \code{conf_high}, \code{p_value},
and optionally \code{n}).}

\item{digits}{Number of decimal places for \code{estimate}, \code{conf_low}, and
\code{conf_high} (default 2).}

\item{p_digits}{Number of decimal places for \code{p_value} (default 3); values
below \code{10^-p_digits} are rendered as \code{"<0.001"} (etc.).}
}
\value{
A data.frame with columns \code{Method}, the outcome-appropriate estimate
label (\code{OR} or \verb{Mean Diff}), \verb{95\% CI}, \code{p-value}, and \code{n}.
}
\description{
Converts a raw \code{comparison} data frame from \code{run_pipeline()}/\code{fit_outcome()}
(full floating-point precision) into a publication-ready table: \code{estimate},
\code{conf_low}, and \code{conf_high} are rounded to \code{digits} decimals, the confidence
interval is rendered as a single \code{"low\\u2013high"} string (en dash), small
p-values are collapsed to \code{"<0.001"} (or the equivalent threshold for
\code{p_digits}), and the estimate column is labeled by outcome type — \code{"OR"} for
\code{type == "binary"}, \code{"Mean Diff"} for \code{type == "continuous"}.
}
\details{
This is a display-layer helper: it intentionally does \strong{not} alter
the underlying data. \code{export_results()} writes CSV files at full numeric
precision for downstream reanalysis; publication-style rounding belongs at
the display layer only (printed tables, vignettes), never baked into
exported data.
}
\examples{
data(example_cohort)
res <- run_pipeline(
  data = example_cohort,
  exposure = "exposure",
  covariates = c("age", "diabetes", "hypertension", "bmi"),
  outcome = "outcome_binary",
  type = "binary",
  methods = c("regression", "matching", "stratification", "iptw", "aipw")
)
format_comparison(res$comparison)
}
