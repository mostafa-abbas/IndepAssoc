% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests_stat.R
\name{mcnemar_test}
\alias{mcnemar_test}
\title{McNemar Test on Matched Data}
\usage{
mcnemar_test(matched_data, outcome, exposure)
}
\arguments{
\item{matched_data}{Matched data frame with \code{match_num} column.}

\item{outcome}{Character; binary outcome variable name, coded 0/1.}

\item{exposure}{Character; binary exposure variable name.}
}
\value{
A data frame with McNemar test results.
}
\description{
Tests for association between a binary \code{exposure} and a binary \code{outcome}
within matched strata, using the Cochran-Mantel-Haenszel test
(\code{stats::mantelhaen.test()}) over the per-stratum 2x2 tables. For 1:1
matching this is equivalent to McNemar's test; it generalizes naturally to
many:1 (ratio > 1) matching, where each stratum may hold several control
units.
}
\details{
Strata (pairs) with no observed outcome on any member cannot
contribute to the test and are dropped; the number dropped is reported via
a \code{message()}. Rows with a missing outcome within otherwise-usable strata
are excluded from that stratum's table and also counted in the message.
If the resulting table is degenerate (e.g. a constant outcome), the test
degrades to an \code{NA} row with a warning rather than halting the pipeline.
}
\examples{
data(example_cohort)
ps <- build_ps_model(example_cohort, "exposure",
                     c("age", "diabetes", "hypertension", "bmi"))
matched <- match_cohort(ps)
mcnemar_test(matched$data, "outcome_binary", "exposure")

}
