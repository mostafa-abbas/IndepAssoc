% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome.R
\name{fit_all_models}
\alias{fit_all_models}
\title{Fit All 3 Outcome Models}
\usage{
fit_all_models(
  ps_model,
  matched_data,
  outcome,
  type = c("binary", "continuous")
)
}
\arguments{
\item{ps_model}{An \code{IndepPSModel} object.}

\item{matched_data}{Matched data frame from \code{match_cohort()}.}

\item{outcome}{Character; outcome variable name.}

\item{type}{\code{"binary"} or \code{"continuous"}.}
}
\value{
A list of class \code{"IndepOutcomeModels"} with \verb{$models}, \verb{$summary}, \verb{$summary_w}.
}
\description{
Fits 3 regression models matching the published pipeline:
\enumerate{
\item Fully adjusted (on full unmatched data)
\item Conditional (clogit/plm on matched data)
\item Mixed effect (glmer/lmer on matched data)
}
}
\examples{
data(example_cohort)
ps <- build_ps_model(example_cohort, "exposure",
                     c("age", "diabetes", "hypertension", "bmi"))
matched <- match_cohort(ps)
models <- fit_all_models(ps, matched$data, "outcome_binary", type = "binary")
models$summary_w

}
