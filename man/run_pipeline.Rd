% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run the IndepAssoc Analysis Pipeline}
\usage{
run_pipeline(
  data,
  exposure,
  covariates,
  outcome,
  type = c("binary", "continuous"),
  caliper = 0.2,
  ratio = 1,
  balance_threshold = 0.1,
  methods = c("regression", "matching", "stratification", "iptw", "aipw")
)
}
\arguments{
\item{data}{Data frame containing the cohort.}

\item{exposure}{Character string naming the binary exposure variable.}

\item{covariates}{Character vector of covariate names.}

\item{outcome}{Character string naming the outcome variable.}

\item{type}{Outcome type: \code{"binary"} or \code{"continuous"}.}

\item{caliper}{Caliper for matching (default \code{0.2}).}

\item{ratio}{Match ratio (default \code{1}).}

\item{balance_threshold}{ASMD threshold (default \code{0.10}).}

\item{methods}{Character vector of confounding-adjustment methods to run
via \code{fit_outcome()} (default all five: \code{"regression"}, \code{"matching"},
\code{"stratification"}, \code{"iptw"}, \code{"aipw"}).}
}
\value{
A list of class \code{"IndepAssoc"} containing all pipeline results.
}
\description{
Orchestrates the full pipeline: PS model, matching, balance check,
descriptive tables, outcome models (3 types), and statistical tests.
}
\examples{
data(example_cohort)
res <- run_pipeline(
  data = example_cohort,
  exposure = "exposure",
  covariates = c("age", "diabetes", "hypertension", "bmi"),
  outcome = "outcome_binary",
  type = "binary",
  methods = c("regression", "matching")
)
res$comparison

}
