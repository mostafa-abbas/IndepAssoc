% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run the IndepAssoc Analysis Pipeline}
\usage{
run_pipeline(
  data,
  exposure,
  covariates,
  outcome,
  type = c("binary", "continuous"),
  caliper = 0.2,
  ratio = 1,
  balance_threshold = 0.1,
  methods = c("regression", "matching", "stratification", "iptw", "aipw"),
  seed = NULL,
  estimand = c("ATE", "ATT")
)
}
\arguments{
\item{data}{Data frame containing the cohort.}

\item{exposure}{Character string naming the binary exposure variable.}

\item{covariates}{Character vector of covariate names.}

\item{outcome}{Character string naming the outcome variable.}

\item{type}{Outcome type: \code{"binary"} or \code{"continuous"}. When omitted it is
auto-detected from the outcome variable: a numeric or logical vector whose
values are all 0/1 is treated as \code{"binary"}, any other numeric vector as
\code{"continuous"}; factor/character outcomes keep the \code{"binary"} default.}

\item{caliper}{Caliper for matching (default \code{0.2}).}

\item{ratio}{Match ratio (default \code{1}).}

\item{balance_threshold}{ASMD threshold (default \code{0.10}).}

\item{methods}{Character vector of confounding-adjustment methods to run
via \code{fit_outcome()} (default all five: \code{"regression"}, \code{"matching"},
\code{"stratification"}, \code{"iptw"}, \code{"aipw"}). \code{"matching"} is propensity-score
matching with conditional-logit (binary) / within-pair (continuous)
estimation; because matching draws on the data, results are reproducible
only when a fixed seed is set before the call. \code{"iptw"} is a marginal
structural model (outcome regressed on the exposure only, weighted by
stabilized inverse probability weights) and \code{"aipw"} is a doubly-robust
augmented estimator.}

\item{seed}{Integer passed to \code{set.seed()} at the top of the pipeline. A
fixed seed makes the whole run — including the step-2 matching and the
step-9 \code{"matching"} method — reproducible from a single value. Default
\code{NULL} (no seeding).}

\item{estimand}{Causal estimand passed to \code{fit_outcome()}: \code{"ATE"} (default;
current behavior) or \code{"ATT"}. With \code{"ATT"}, \code{"iptw"} and \code{"aipw"} use
standardized mortality ratio (SMR) weights and \code{"stratification"} weights
each stratum's effect by the number of treated units, so the
propensity-score methods target the average treatment effect on the
treated (Austin 2011, doi:10.1080/00273171.2011.568786). \code{"matching"}
always targets the ATT by construction and ignores this argument.}
}
\value{
A list of class \code{"IndepAssoc"} containing all pipeline results,
including \code{balance_plot} — the \code{ggplot} chart of absolute standardized mean
differences (ASMD) for unadjusted vs. matched cohorts, produced by
\code{plot_asmd_balance()} at the \code{balance_threshold} used — and \code{positivity},
the \code{IndepPositivity} object from \code{check_positivity()} (propensity-score
overlap and IPTW weight diagnostics for the requested \code{estimand}).
}
\description{
Orchestrates the full pipeline: PS model, matching, balance check,
descriptive tables, outcome models (3 types), and statistical tests.
}
\details{
Whenever \code{"matching"} is among the requested \code{methods}, a fixed
\code{seed} is required for reproducible results. Pass the same \code{seed} value to
\code{run_pipeline()} rather than setting a seed mid-pipeline, so the step-2
matching and the step-9 \code{"matching"} method both draw on it.

The ASMD balance chart is returned as \code{result$balance_plot} for you to print
or save; \code{print()} does not render it.
}
\examples{
data(example_cohort)
res <- run_pipeline(
  data = example_cohort,
  exposure = "exposure",
  covariates = c("age", "diabetes", "hypertension", "bmi"),
  outcome = "outcome_binary",
  type = "binary",
  methods = c("regression", "matching")
)
res$comparison

}
