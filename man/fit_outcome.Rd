% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome.R
\name{fit_outcome}
\alias{fit_outcome}
\title{Fit Outcome Model on Matched Cohort}
\usage{
fit_outcome(
  match_obj,
  outcome,
  type = c("binary", "continuous", "time_to_event"),
  time_var = NULL,
  event_var = NULL,
  covariates = NULL,
  weights = NULL
)
}
\arguments{
\item{match_obj}{An \code{IndepMatch} object from \code{match_cohort()}.}

\item{outcome}{Character string naming the outcome variable.}

\item{type}{One of \code{"binary"}, \code{"continuous"}, or \code{"time_to_event"}.}

\item{time_var}{Character; name of the time variable (required if \code{type = "time_to_event"}).}

\item{event_var}{Character; name of the event indicator (required if \code{type = "time_to_event"}).}

\item{covariates}{Optional additional covariates to adjust for beyond exposure.}

\item{weights}{Optional numeric vector of case weights (e.g., IPTW).}
}
\value{
A list of class \code{"IndepOutcome"} with elements:
\describe{
\item{model}{The fitted model object.}
\item{tidy}{Tidy \code{broom::tidy()} output with estimate, CI, p-value.}
\item{type}{The outcome type used.}
}
}
\description{
Regresses the outcome on the exposure (and optionally covariates)
in the matched cohort, dispatching by outcome type.
}
