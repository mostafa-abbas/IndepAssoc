% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance.R
\name{plot_love}
\alias{plot_love}
\title{Love Plot: ASMD Before and After Matching}
\usage{
plot_love(
  match_obj,
  threshold = 0.1,
  title = NULL,
  unmatched_color = "#4472C4",
  matched_color = "#ED7D31"
)
}
\arguments{
\item{match_obj}{An \code{IndepMatch} object from \code{match_cohort()}.}

\item{threshold}{Numeric; horizontal threshold line (default \code{0.10}).}

\item{title}{Character; plot title.}

\item{unmatched_color}{Color for unmatched bars (default \code{"#4472C4"} blue).}

\item{matched_color}{Color for matched bars (default \code{"#ED7D31"} orange).}
}
\value{
A \code{ggplot} object (invisible).
}
\description{
Creates a vertical bar chart comparing absolute standardized
mean differences (ASMD) for each covariate before and after matching.
}
\examples{
data(example_cohort)
ps <- build_ps_model(example_cohort, "exposure",
                     c("age", "diabetes", "hypertension", "bmi"))
matched <- match_cohort(ps)
plot_love(matched, threshold = 0.10)

}
