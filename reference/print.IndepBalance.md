# Print an IndepBalance summary

Print an IndepBalance summary

## Usage

``` r
# S3 method for class 'IndepBalance'
print(x, ...)
```

## Arguments

- x:

  An `IndepBalance` object from
  [`check_balance()`](https://mostafa-abbas.github.io/IndepAssoc/reference/check_balance.md).

- ...:

  Additional arguments (ignored).
